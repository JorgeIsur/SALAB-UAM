<div class="row">
    <div class="col-md-4 mx-auto">
        <div class="card mt-4 text-center">
            <div class="card-header"><h2>Registro de usuarios</h2></div>
            <img src="/pictures/index.jpg" class="logo mx-auto d-block m-3">
            <div class="card-body">
                <form action="/users/signup" method="POST">
                    <div class="mt-3">
                        <input type="text"class="border border-primary" name="name" placeholder="   Nombre" value="{{name}}">
                    </div>
                    <div class="mt-3">
                        <input type="text"class="border border-primary" name="matricula" placeholder="Matricula" value="{{matricula}}">
                    </div>
                    <div class="mt-3">
                        <input type="email"class="border border-primary" name="email" placeholder="Correo Electrónico" value="{{email}}">
                    </div>
                    <div class="mt-3">
                        <input type="password"class="border border-primary" name="password" placeholder="Contraseña" value="{{password}}">
                    </div>
                    <div class="mt-3">
                        <input type="password"class="border border-primary" name="confirm_password" placeholder="Confirmación de contraseña">
                    </div>
                    <div class="form-group mt-3">
                        <div class="form-check form-switch">
                            <input class="form-check-input" type="checkbox" id="admin" name="admin" value="true" disabled>
                            <label class="form-check-label" for="admin">Administrador</label>
                        </div>
                    </div>
                    <div class="mt-3">
                        <button class="btn btn-primary w-100" type="submit">
                            Registro
                        </button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>
